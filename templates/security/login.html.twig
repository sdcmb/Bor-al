{% extends 'base.html.twig' %}

{% block body %}
  <h1>Connexion</h1>

  <form action="{{ path('security_login') }}" method="post">
    <div class="form-group">
      Identifiant : <input plaeholder="Adresse email" required name="_username" type="text" class="form-control">
      <br>
      Mot de passe : <input plaeholder="Mot de passe" required name="_password" type="password" class="form-control">
      <br>
      <input type="checkbox" id="remember_me" name="_remember_me"/> Se souvenir de moi
    </div>
    <div class="form-group">
      Vous n'avez pas encore de compte ? <a href="{{ path('security_registration') }}">Inscrivez vous !</a>
      <button type="submit" name="btn btn-success">Se connecter</button>
    </div>
  </form>
{% endblock %}
